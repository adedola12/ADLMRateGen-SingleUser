<UserControl x:Class="ADLMRateGen.View.CarbonRateItemDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650"
             d:DesignWidth="760">
    <!--──────── LAYOUT ────────-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- HEADER -->
        <DockPanel Margin="0 0 0 18"
                   Grid.Row="0">
            <Button DockPanel.Dock="Left"
                    Style="{StaticResource IconButton}"
                    Click="BackButton_Click" />
            <StackPanel>
                <TextBlock Text="Rate Composition"
                           Style="{StaticResource Headline}" />
                <TextBlock Text="{Binding Description}"
                           Style="{StaticResource Body}"
                           Width="660"
                           TextWrapping="Wrap" />
            </StackPanel>
        </DockPanel>
        <Border Grid.Row="1"
                CornerRadius="12">
            <Grid>
                <DataGrid ItemsSource="{Binding BreakdownLines}"
                          Margin="1"
                          Style="{StaticResource DetailGrid}">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="FontWeight"
                                    Value="Normal" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsTotalLine}"
                                             Value="True">
                                    <Setter Property="FontWeight"
                                            Value="Bold" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Component"
                                            Binding="{Binding ComponentName}"
                                            Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"
                                       BasedOn="{StaticResource WrapCellText}" />
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Quantity"
                                            Binding="{Binding Quantity}"
                                            Width="90" />
                        <DataGridTextColumn Header="Unit"
                                            Binding="{Binding Unit}"
                                            Width="100" />
                        <DataGridTextColumn Header="Unit Price"
                                            Binding="{Binding UnitPrice, Converter={StaticResource MoneyConv}, StringFormat=N2}"
                                            Width="150" />
                        <DataGridTextColumn Header="Total Cost"
                                            Binding="{Binding TotalPrice, Converter={StaticResource MoneyConv}, StringFormat=N2}"
                                            Width="150" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
