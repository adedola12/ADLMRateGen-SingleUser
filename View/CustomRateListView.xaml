<UserControl x:Class="ADLMRateGen.View.CustomRateListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADLMRateGen.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10" Background="White">
        <Grid.RowDefinitions>
            <!-- 1. Header -->
            <RowDefinition Height="Auto"/>
            <!-- 2. Search Row -->
            <RowDefinition Height="Auto"/>
            <!-- 3. DataGrid -->
            <RowDefinition Height="*"/>
            <!-- 4. Delete Button -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Saved Custom Rates" FontSize="30" FontWeight="Bold"/>
            <TextBlock Text="View, search or delete your custom rates." FontSize="15"/>
        </StackPanel>

        <!-- 2. Search Row -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Search:" FontSize="15" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="300" Height="30"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                     ToolTip="Search by Title or Description"/>
        </StackPanel>

        <!-- 3. DataGrid of Rates -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding RatesView}" 
                  SelectedItem="{Binding SelectedRate, Mode=TwoWay}"
                  AutoGenerateColumns="False" 
                  Margin="0,10,0,10"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Width="Auto" 
                                    Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Description" Width="*" 
                                    Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Overhead (%)" Width="Auto" 
                                    Binding="{Binding OverheadPercent}"/>
                <DataGridTextColumn Header="Profit (%)" Width="Auto" 
                                    Binding="{Binding ProfitPercent}"/>
                <DataGridTextColumn Header="Grand Total" Width="Auto" 
                                    Binding="{Binding GrandTotal, StringFormat={}{0:N2}}"/>
                <DataGridTextColumn Header="Date" Width="Auto" 
                                    Binding="{Binding CreatedDate}"/>
                <DataGridTemplateColumn Header="Actions" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="View"
                                Command="{Binding DataContext.ViewRateCommand,
                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <!-- 4. Delete Button -->
        <Button Grid.Row="3" Content="Delete Selected Rate"
                Command="{Binding DeleteRateCommand}" 
                Margin="0,10,0,0"
                Width="200" Height="40" 
                FontSize="15" FontWeight="Bold"
                HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
