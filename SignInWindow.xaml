<Window x:Class="ADLMRateGen.SignInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:ADLMRateGen.View"
        mc:Ignorable="d"
        Title="ADLM Rate Gen"
        Width="1280"
        Height="860"
        MinWidth="1100"
        MinHeight="760"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True">

    <Window.Resources>
        <Style x:Key="ChromeButtonStyle"
               TargetType="Button">
            <Setter Property="Width"
                    Value="34" />
            <Setter Property="Height"
                    Value="34" />
            <Setter Property="Margin"
                    Value="0,0,0,0" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Foreground"
                    Value="#DCE9FF" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="FontFamily"
                    Value="Poppins" />
            <Setter Property="FontSize"
                    Value="15" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ChromeButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="ChromeButtonBorder"
                                        Property="Background"
                                        Value="#223A63" />
                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="ChromeButtonBorder"
                                        Property="Background"
                                        Value="#182B48" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <view:SignInUserControl />

        <Border HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="18,14,0,0"
                Padding="14,8"
                Background="#D90B1729"
                CornerRadius="16"
                Panel.ZIndex="2"
                MouseLeftButtonDown="TitlePill_MouseLeftButtonDown">
            <TextBlock Text="ADLM Rate Gen"
                       Foreground="#EDF4FF"
                       FontFamily="Poppins"
                       FontSize="13"
                       FontWeight="SemiBold" />
        </Border>

        <Border HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,14,18,0"
                Padding="6"
                Background="#D90B1729"
                CornerRadius="16"
                Panel.ZIndex="2">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource ChromeButtonStyle}"
                        ToolTip="Minimize"
                        Click="Minimize_Click"
                        Content="&#9472;" />
                <Button Style="{StaticResource ChromeButtonStyle}"
                        Margin="6,0,0,0"
                        ToolTip="Close"
                        Click="Close_Click"
                        Content="&#10005;" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
